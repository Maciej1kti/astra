# Planning visual review — 2026-09-08

Reviewed the final seven Chromium screenshots and assertions from the custom
run beginning `2026-09-08T07:14:39.439Z`, following the earlier five-image review.
**Result: the audited calendar presentation, navigation and timeline selection
checks pass on this release in the tested browser.** This is bounded acceptance
of these scenarios, not a claim that every planning behavior or device is verified.

- **Desktop month:** readable and bounded at 742 px with 56 dated items. Real
  `+N more` links fit inside busy days; empty weeks remain compact. Link contrast
  and the Today numeral are now readable. Document width stays at 1440 px.
- **Desktop overflow:** the day dialog displays the hidden items with readable
  labels and wrapping titles. Its close control has a large target and a verified
  accessible name. The dialog remains within the calendar region.
- **Desktop workspace Today:** the date input and emphasized September 8 day agree
  with the workspace header while the Honolulu browser's local date is September
  7. Planned work, deadlines and review entries remain visually distinct.
- **Phone month grid:** the alternate grid remains inside its horizontal scroll
  region. Document width is 390 px. Toolbar controls fit, and overflow links stay
  visible. The grid intentionally requires horizontal scrolling; full titles are
  available in day details and the agenda alternative.
- **Phone agenda — verified fix:** the final screenshot shows readable event
  titles and deadline/plan labels beneath day headings. Its region remains 606 px
  high, while 109 dated occurrences occupy 6,986 px of internal scrolling.
  Both the first and last event titles passed viewport assertions and their
  buttons passed unobscured trial clicks after scrolling. Document width remains
  390 px. The earlier run's date-header-only rendering was a real defect: CSS
  grid plus zero minimum day height compressed rows and covered their content.
  Normal block flow inside the bounded scroll region fixes that failure.
- **Phone timeline — verified selection:** the full selected title, recorded
  plan and hard deadline remain readable above the chart after horizontal
  panning. The chart in the screenshot is deliberately panned to the right edge,
  which hides its title column and truncates bars. The independent selection
  summary retains identity and its Open card / Edit planned dates actions. The
  assertion confirmed unchanged summary position and width during the pan.
- **Desktop timeline:** the title column, shared date axis, bars, dependency
  lines and full selected-item summary are readable. Coverage and incomplete
  forecast warnings remain visible. The long document includes the expanded
  unscheduled list generated by this populated synthetic fixture.

The earlier helper incorrectly reported `timelineSelection: true` when the
fixture ID was present but its title was missing and timeline assertions were
skipped. The runner now reads the saved title through CLI and rejects an empty
title; helper reporting uses the full condition. The final run produced both
timeline screenshots and exercised all their assertions, superseding that
earlier false positive.

The final run recorded no page exceptions, console errors, CSP violations,
failed requests or HTTP errors. Calendar reload and Back/Forward assertions
passed. Workspace Today remained September 8 while the Honolulu browser date
was September 7. See [results.json](https://github.com/Maciej1kti/astra/blob/2a5530a8bb83eec0c3f6f289cac8587aa9d66898/progress/fixes-2026-09-08/planning-browser/results.json).

The agenda full-page capture places the sticky navigation midway down the
document because the preceding unobscured-click assertion had scrolled the outer
page. This is a screenshot-state artifact; it is not evidence that navigation
occupies that document position during normal top-of-page viewing. The capture
helper now returns the outer page to the top before its two-frame settling step
for future screenshots. It preserves each widget's internal scroll state. That
capture-only adjustment was not used to claim a new test run.

All phone evidence is emulated; physical iPhone behavior remains unverified.
